#include <cs50.h>
#include <stdio.h>

int main(void)
{

    int n = strlen()
    while (input_list) // repeat until no swaps
    {
        for (int i=0; i < n-2; i++) // for i from 0 to n-2
        {
            if () // if i'th and i+1'th elements out of order
            {
                // swap them
            }
        }
    }
}

